<h1>New Message media</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.message_media_path(@conn, :create)) %>

<span><%= link "Back", to: Routes.message_media_path(@conn, :index) %></span>
